% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add.R
\name{add_plane_by_position_and_normal}
\alias{add_plane_by_position_and_normal}
\title{Add a plane defined by position and normal}
\usage{
add_plane_by_position_and_normal(
  normal,
  position,
  color_plane = "orange",
  lit = FALSE,
  alpha = 0.5,
  show_normal = TRUE,
  arrow_color = color_plane,
  arrow_type = "rotation",
  arrow_theta = pi/6,
  arrow_alpha = 1,
  arrow_lit = lit,
  add_invisible_point = TRUE,
  ...
)
}
\arguments{
\item{normal}{Numeric length-3 vector giving the plane normal.}

\item{position}{Numeric length-3 vector specifying a point on the plane.}

\item{color_plane}{colour of plane.}

\item{lit}{Logical; if \code{TRUE}, the plane and arrows are affected by scene lighting.}

\item{alpha}{Numeric in \verb{[0, 1]} giving plane transparency.}

\item{show_normal}{Logical; if \code{TRUE}, draws one or more arrows indicating
the plane normal direction.}

\item{arrow_color}{colour of normal arrow}

\item{arrow_type}{value of rgl::arrow3d type argument.}

\item{arrow_theta}{value of rgl::arrow3d theta argument.}

\item{arrow_alpha}{arrow alpha.}

\item{arrow_lit}{should arrow be affected by lighting.}

\item{add_invisible_point}{add an invisible point in the plane to guarantee it gets rendered (rgl calculates viewbox based on finite elements only, ignoring infinite elements like planes causing them to not get rendered)}

\item{...}{
  Arguments passed on to \code{\link[=add_plane]{add_plane}}
  \describe{
    \item{\code{offset}}{Numeric scalar giving the plane's offset (distance from the origin
along the normal).}
  }}
}
\value{
Invisibly returns a length-2 list with the rgl shape Ids for the plane and arrow objects (named plane and arrow respectively)
}
\description{
Computes the plane offset from a point and normal, then draws it with
\code{\link[=add_plane]{add_plane()}}. Accepts the same visual arguments as add_plane().
}
\seealso{
\code{\link[=add_plane]{add_plane()}}
}
