---
title: "Manual"
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(rgl)
library(move)
library(rglhelper)
setupKnitr()
options(rgl.useNULL = FALSE)
```

## Add Planes

```{r}
open3d()
# points3d(0, 0, 0, alpha = 0, lit=FALSE)
points3d(0, 0, 0, alpha = 0, lit=FALSE)
# rgl::arrow3d(p0 = c(0, 0, 0), p1 = c(0, 1, 0), type = "rotation", width = 0.2, n=8, smooth = TRUE, lit=FALSE, color="red", s = 0.2)
# rgl::arrow3d(p0 = c(0, 0, 0), p1 = c(0, 1, 0), type = "extrusion", width = 0.1, thickness = 0.5,smooth = TRUE, alpha=1, lit=TRUE, color="red")
add_plane(normal = c(0, 1, 0), )
rglwidget(minimal = TRUE)
```

